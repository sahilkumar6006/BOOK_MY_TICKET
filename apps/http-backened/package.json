{
  "name": "http-backened",
  "version": "1.0.0",
  "description": "A Node.js backend app using Express and esbuild, ready for production.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development npm run build && node dist/index.js",
    "build": "esbuild ./src/index.ts --bundle --platform=node --minify --outfile=dist/index.js",
    "start": "cross-env NODE_ENV=production node dist/index.js",
    "vercel-build": "pnpm build",
    "vercel-dev": "pnpm dev",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "ISC",
  "dependencies": {
    "@repo/db": "workspace:*",
    "@types/authenticator": "^1.1.4",
    "@types/express": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.8",
    "authenticator": "^1.1.5",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "esbuild": "^0.17.16",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "twilio": "^5.4.2"
  },
  "devDependencies": {
    "@repo/common": "workspace:*",
    "@repo/redis": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/cors": "^2.8.19",
    "cross-env": "^7.0.3",
    "typescript": "^5.8.2"
  }
}
